import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Brand
  static const Color primary = Color(0xFFFF7A59);
  static const Color primaryLight = Color(0xFFFFE8E2);
  static const Color accent = Color(0xFFFFD8CC);

  // Backgrounds
  static const Color scaffold = Color(0xFFFFF9F7);
  static const Color card = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF232323);
  static const Color textSecondary = Color(0xFF777777);

  // Status
  static const Color success = Color(0xFF2DBF64);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFE53935);

  // Borders
  static const Color border = Color(0xFFE7E7E7);

  // Chips
  static const Color chipBackground = Color(0xFFF6F6F6);

  // Input
  static const Color inputFill = Colors.white;
}